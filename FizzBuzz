#include <iostream>

int main() {

  // Write a for loop here:
  
  for (int i = 1; i <= 100; i++) {
    
    int a = i % 3;
    int b = i % 5;
    
    if (a == 0 && b == 0) {
      
      std::cout << "FizzBuzz \n";
      
    }
    
    else if (a == 0 && b != 0) {
      
      std::cout << "Fizz \n";
      
    }
    
    else if (b == 0 && a != 0) {
      
      std::cout << "Buzz \n";
      
    }
    
    else {
      
      std::cout << i << "\n";
      
    }
    
  }
  }
